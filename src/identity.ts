// Deprecated: identity logic removed. Kept as no-op shim to avoid stale imports during refactor.
export const computeShipIdentity = (_prompt: string) => {
  throw new Error("computeShipIdentity is deprecated after simplification");
};
